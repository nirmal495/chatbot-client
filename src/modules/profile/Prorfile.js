import React from 'react';
import Nav from '../common/Nav';

const Profile = (props) => {
    return (
        <>
            <Nav />
            This is profile
        </>
    );
};

export default Profile;
